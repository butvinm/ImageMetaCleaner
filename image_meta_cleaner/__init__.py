"""Image Meta Cleaner.

An utility to clean image metadata and extract location info.

It is a command line tool that takes a directory as input and
recurse through all the files in the directory and subdirectories
and clean the metadata of all the images.

It also saves the location info of the images in a csv files with same
name as the image file.
"""
